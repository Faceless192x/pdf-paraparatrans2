# シンボルフォント登録機能

## 機能の目的

PDFによっては、見た目は「記号」でも実体は英数字であるシンボルフォントが使用されています。  
この状態のまま翻訳すると、文章が崩れたり意味不明な文字が混ざる場合があります。

シンボルフォント登録機能では、

- 特定フォントの特定文字を
- 任意の文字列へ
- 事前に置換

できます。

## できること

- 記号フォントの文字を、読みやすい記号や語句に置き換える
- 翻訳前に不要な記号を別の目印文字へ置き換える
- ブックごとに、使用フォントから対象を選んで登録する

## 使い方

1. 対象ブックの画面を開きます。  
2. **シンボルフォント登録** を開きます。  
3. 左の **フォント一覧** から対象フォントを選びます。  
4. 右側の表で、`フォント名.文字` ごとに **置換後文字列** を入力します。  
5. **保存** を押します。  
6. 必要に応じて (シンボル置換) を実行し、置換結果を反映します。

## 入力ルール

- キーは `フォント名.文字` の形式です（例: `Wingdings.a`）。
- 文字が `.`（ピリオド）の場合は `フォント名..` になります。
- 置換後文字列は自由に設定できます（空欄は不可）。
	- 例: `■`
	- 例: `[Storm]`

## 例

- `GloranthaRuneIcons.a` → `風`
- `Wingdings.b` → `◆`
- `SymbolFont..` → `・`

## 注意点

- 置換設定を保存しただけでは、既存のテキストに即時反映されない場合があります。  
	必要に応じて再構築処理を実行してください。
- 同じ `フォント名.文字` を再登録すると、以前の設定は新しい設定で上書きされます。

