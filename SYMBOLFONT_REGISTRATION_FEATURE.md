# シンボルフォント登録機能の実装完了

## 概要

PDFブック内のスタイル一覧を表示し、フォントを選択して`symbolfont_dict.txt`と`symbolfonts.txt`に登録する機能を追加しました。

## 実装内容

### 1. UI コンポーネント

#### ボタン追加
- **ファイル**: `templates/detail.html`
- **追加箇所**: ヘッダーアクションエリア
- **ボタン ID**: `openSymbolFontsButton`
- **ボタンテキスト**: "シンボルフォント登録"

#### モーダルダイアログ
- **ID**: `symbolFontsDialog`
- **機能**:
  - ステップ 1: ブック内のスタイル一覧を表示
  - ステップ 2: フォントを選択して置換文字列を入力し登録
  - 登録済みシンボルの管理（表示・削除）

### 2. JavaScript コンポーネント

#### ファイル: `static/js/symbolFonts.js`

**主要な関数**:

1. **openSymbolFontsDialog()** - ダイアログを開く
2. **closeSymbolFontsDialog()** - ダイアログを閉じる
3. **loadBookStyles()** - ブック内のスタイル一覧を取得して表示
4. **selectFontStyle(styleKey, styleValue)** - フォントスタイルを選択
5. **registerSymbolFont()** - シンボルフォントを登録
6. **loadRegisteredSymbols()** - 登録済みシンボルを読み込む
7. **deleteSymbolFont(key)** - シンボルフォントを削除

### 3. Flask API エンドポイント

#### ファイル: `pdf-paraparatrans.py`

**新規エンドポイント**:

1. **GET `/api/book_styles/<pdf_name>`**
   - ブック内のすべてのスタイルを取得
   - レスポンス: `{"status": "ok", "styles": {...}}`

2. **POST `/api/register_symbolfont`**
   - シンボルフォントを登録
   - ペイロード: `{"font_style": "フォント名", "replacement": "置換文字列"}`
   - 処理内容:
     - `symbolfont_dict.txt` に登録
     - `symbolfonts.txt` にフォント名を追加（重複排除）

3. **GET `/api/get_registered_symbolfonts`**
   - 登録済みシンボルフォント一覧を取得
   - レスポンス: `{"status": "ok", "symbols": {...}}`

4. **POST `/api/delete_symbolfont`**
   - シンボルフォントを削除
   - ペイロード: `{"key": "フォント.キャラクター"}`
   - 対象: `symbolfont_dict.txt` から削除

### 4. CSS スタイル

#### ファイル: `static/css/symbolFonts.css`

- モーダルダイアログのテーブルスタイリング
- ボタンやインタラクティブ要素のスタイル
- ホバーエフェクト
- カラースキーム（削除ボタンは赤色）

## 使用方法

### ステップ 1: ボタンをクリック
詳細画面のヘッダーアクションエリアにある「シンボルフォント登録」ボタンをクリックします。

### ステップ 2: スタイル一覧を表示
ダイアログが開いたら、「スタイル一覧を表示」ボタンをクリックしてブック内のスタイル一覧を読み込みます。

### ステップ 3: フォントを選択
表示されたスタイル一覧から登録したいフォント（例: `Wingdings.a`）の「選択」ボタンをクリックします。

### ステップ 4: 置換文字列を入力して登録
- 「選択中のフォント」にスタイルキーが表示されます
- 「置換後の文字列」に置換文字列を入力（例: `■` や `[Symbol1]`）
- 「登録」ボタンをクリック

### ステップ 5: 確認
下部の「登録されたシンボル一覧」に登録内容が表示されます。

### 削除する場合
登録されたシンボル一覧から削除したい項目の「削除」ボタンをクリックして確認します。

## ファイルシステムの変更

### symbolfont_dict.txt
```
フォント名.キャラクター<TAB>置換後文字列
```

例:
```
Wingdings.a	■
Wingdings.b	▲
GloranthaCoreRunes.1	[Ф1]
```

### symbolfonts.txt
```
Wingdings
Webdings
GloranthaCoreRunes
```

## エラーハンドリング

- JSONファイルが存在しない場合: エラーメッセージを表示
- フォント名が選択されていない場合: アラート表示
- 置換文字列が入力されていない場合: アラート表示
- API呼び出し失敗時: エラーメッセージをダイアログに表示

## 技術仕様

### フロントエンド
- Vanilla JavaScript
- 非同期通信: Fetch API
- UIフレームワーク: 既存の`data-export-dialog`クラスを再利用

### バックエンド
- Flask
- ファイルI/O: UTF-8 エンコーディング
- ファイル操作: 安全な上書き（一時ファイル使用なし）

## 注記

1. **フォント名の正規化**: `symbolfont_dict.txt` の読み込み時にフォント名は大小文字や空白の差を吸収します
2. **翻訳タグの対応**: 置換文字列には翻訳拒否タグなどを含めても問題ありません
3. **ファイルエンコーディング**: すべてのファイル操作は UTF-8 エンコーディングで実行されます
4. **自動バックアップ**: ユーザー操作は直接ファイルを上書きするため、事前のバックアップを推奨します

## トラブルシューティング

###「スタイルが見つかりません」と表示される場合
- ブックがまだパラグラフ抽出を完了していない
- JSONファイルが破損している

### 登録したシンボルが反映されない場合
- ブックの「シンボル置換」処理を再度実行してください
- PDFを再度抽出してください

### ファイルが読み取り専用として表示される場合
- ファイルアクセス権限を確認してください
- アプリケーションを管理者権限で実行してください
